{
  "_comment": "Copy to ~/.margie/config.json (chmod 600). Omit any key you don't use \u2014 the scripts treat a missing key as 'not configured', but an empty string is NOT treated as missing by jq.",
  "engine": "claude",
  "warp_mode": "quiet",
  "deploy_watch": "on",
  "_deploy_watch": "on = Margie watches a production deploy Tom/Cody triggers (she never starts one) via the repo deploy-watcher agent and reports the verdict. deploy_environment (production), deploy_app (AppSignal app, WaltUI/prod), deploy_autorollback (false = watch-only, escalate; true = let the agent roll back a migration-additive deploy), deploy_watch_minutes (30).",
  "deploy_environment": "production",
  "deploy_app": "WaltUI/prod",
  "deploy_autorollback": false,
  "deploy_watch_minutes": 30,
  "session_narrate": "on",
  "_session_narrate": "on = Margie gives a running play-by-play of each Claude coding session (announces every new \u23fa action it takes); off = only stuck/question/ended notices.",
  "_warp_mode": "window = every session opens its own Warp window; quiet = sessions run in tmux with no window and are watched from the margie CLI (/watch <name>) or `session.sh attach <name>`.",
  "claude_model": "claude-fable-5-1[1m]",
  "_claude_model": "Default --model for EVERY Claude Code run Margie dispatches (interactive sessions, reviews, headless tasks, planner, QA). Omit to use the CLI's own default. planner_model / qa_model override it per stage.",
  "_engine": "CLI the dispatched sessions run (kickoff-claude.sh / review-pr.sh / simulate.sh): 'claude' (default, Claude Code) or 'grok'. Overridden by MARGIE_ENGINE. Omit unless you want grok.",
  "forge": "gitlab",
  "gitlab_host": "gitlab.com",
  "org": "your-group-or-org",
  "repos_dir": "~/repos",
  "default_repo": "backend",
  "review_skill": "",
  "_forge": "forge: gitlab | github \u2014 which CLI (glab / gh), vocabulary (MR / PR) and write-guards Margie uses. Log in once with `glab auth login` or `gh auth login`. gitlab_host: only for self-hosted GitLab. org: GitLab group (subgroups included) or GitHub org bare repo names resolve against \u2014 omit to search the projects you're a member of.",
  "_repos": "repos_dir: where your clones live (bare repo names resolve here; new clones land here). default_repo: assumed when Tom says 'review MR 12' without naming one \u2014 omit to make her ask. review_skill: a repo-local skill name review-pr.sh should invoke \u2014 omit for a generic thorough-review prompt.",
  "brain_voice_backend": "xai",
  "brain_backend": "claude",
  "brain_claude_model": "sonnet",
  "_brain": "Which model IS Margie, per channel: voice (the overlay) uses brain_voice_backend \u2014 xai for sub-second turns; terminal/Slack use brain_backend \u2014 'claude' runs on your Claude plan via the Agent SDK (no API key), model brain_claude_model (sonnet is snappy; fable for depth). Both share one history and the same guarded bash tool.",
  "typesafe_api_key": "apikey_...",
  "jev": "on",
  "jev_model": "jev-latest",
  "_jev": "TypeSafe's Jev (docs.typesafe.ai): a fast calibrated classifier the HARNESS uses for small typed decisions the code used to guess with regexes \u2014 is an idle coding session really asking something (session.sh needs, before waking the brain), is a Slack mention actually addressed to Margie/the owner (slack-watch.sh), is a session's permission prompt risky (a second opinion that can only escalate), was Tom's short reply a yes/no to a held command (the confirm gate, high confidence only), which dispatch a status question is about. Never talks, never runs anything; every decision fails closed to the old path and is logged in ~/.margie/jev.log. jev: off disables it (MARGIE_JEV=off too). Key: console.typesafe.ai/keys.",
  "xai_api_key": "xai-...",
  "xai_model": "grok-4.20-0309-non-reasoning",
  "_xai": "Margie's conversational brain (sidecar/src/index.ts) runs on the xAI chat API. Required for her to answer at all.",
  "slack_via": "claude",
  "slack_cc_owner": true,
  "slack_watch": "preview",
  "standup_channel": "#standup",
  "standup_time": "08:45",
  "standup_mode": "draft",
  "_standup": "Margie drafts the owner's daily standup from git/MR/ticket evidence on weekdays at standup_time: mode draft = DM the draft and wait for 'post my standup'; post = post it herself; off.",
  "slack_owner_id": "U0AAAAAAAAA",
  "owner_first_name": "Tom",
  "telnyx_api_key": "",
  "_telnyx": "Optional. A Telnyx v2 API key lets telnyx.sh buy/assign numbers and send test (group) MMS for spikes; telnyx_number and telnyx_profile_id are filled in by the script.",
  "pronouns": {
    "Cody Poll": "they/them"
  },
  "conversation_notes": {
    "C0XXXXXXXXX": "Group DM with X. X asked: track work in Notion, don't post status here; reply only when addressed."
  },
  "_slack_watch": "Watcher for @Margie and @<owner> mentions in channels the bot is in: off | preview (drafts DM'd to the owner, nothing posted) | live (replies in-thread as the owner's assistant). slack_owner_id: the owner's Slack user id (falls back to the agent roster).",
  "slack_user_token": "xoxp-...",
  "slack_token": "xoxb-...",
  "_slack2": "Sends ALWAYS go out as the Margie bot (slack_token) \u2014 never through the Claude connector, whose posts carry a 'Sent using @Claude' footer as the owner. Reads/search use the connector (as the owner) for reach.",
  "_slack": "slack.sh read/send default to Claude Code's Slack connector (the claude.ai Slack app \u2014 run `claude mcp list` to confirm it's connected) when no token is set; slack_via: 'claude' forces it even with tokens. Tokens: user token = direct Web API as Tom; bot token = posts as @margie and is REQUIRED for the slack-watch.sh @mention watcher (that one doesn't use the connector).",
  "gmail_address": "you@example.com",
  "gmail_app_password": "16-char app password (myaccount.google.com/apppasswords)",
  "notion_token": "ntn_...",
  "notion_parent_page": "https://www.notion.so/...",
  "notion_drafts_parent": "<page id>",
  "notion_status_parent": "",
  "_notion_status": "Auto-created by status-sync.sh: the \"Margie \u2014 Status\" page her per-project state is mirrored to (backup + team-visible). Leave empty; it fills in on first sync.",
  "_notion_drafts": "Page under which dispatch.sh publishes every finished spec as a read-only draft page (so the owner can read the plan in Notion before saying go). Replaced on amend; the real ticket + spec page are created on go.",
  "_notion": "Internal integration token (notion.so/my-integrations; name it 'Margie'). It only sees pages connected to it: page \u2192 \u00b7\u00b7\u00b7 \u2192 Connections \u2192 Margie. notion_parent_page: where `notion.sh create` puts new pages when no --parent is given.",
  "atlassian_email": "you@example.com",
  "atlassian_api_token": "...",
  "jira_base_url": "https://yourorg.atlassian.net",
  "jira_default_project": "KEY",
  "notion_tickets_ds": "\u202632-hex data source id\u2026",
  "notion_tickets_db": "\u2026database id\u2026",
  "notion_testcases_ds": "\u2026",
  "notion_usecases_ds": "\u2026",
  "notion_requirements_ds": "\u2026",
  "notion_decisions_db": "\u2026",
  "notion_open_questions_db": "\u2026",
  "notion_docs_parent": "",
  "notion_ticket_defaults": {
    "Status": "Planning",
    "Team": "Product",
    "Labels": [
      "Claude"
    ],
    "Priority": "Medium"
  },
  "notion_assignee": "\u2026notion person id\u2026",
  "ticket_prefix": "PT",
  "branch_prefix": "margie",
  "repo_subdirs": {
    "walt_ui": "backend"
  },
  "planner_model": "sonnet",
  "qa_model": "sonnet",
  "planner_effort": "medium",
  "qa_effort": "medium",
  "planner_subagents": false,
  "_process": "Team process notes: write ~/.margie/process/<repo>.md (how MRs, review bots, agent reviewers and merges work in that repo). Injected into every Claude brain turn and into the implement/QA/review prompts. Company-specific content stays out of the codebase.",
  "_budgets": "Every headless Claude run gets --max-budget-usd: dispatch_budget_usd for planner/QA (default 4), task_budget_usd for other tasks (default 5). daily_budget_usd (optional) refuses new headless launches once usage.sh reports today's spend at or above it.",
  "auto_merge": true,
  "_auto_merge": "tick merges an MR itself once its pipeline is green and every review thread is resolved (and the self-review approved or its rounds are used up). false = announce 'ready to merge' and wait for the owner's word.",
  "auto_deploy_label": "Auto-Deploy",
  "_auto_deploy_label": "The CI label that gates a production deploy of a merged MR (release:create ships only a merged MR carrying this label, or a manual play; labels are read live at deploy time). mr.sh merge adds it at merge on both paths \u2014 backend auto-merge and the owner's explicit 'merge' for UI/chat (his 'merge' is his approval to ship) \u2014 but NEVER on a High Risk MR (those merge and wait for the owner to add the label / play the deploy). Empty = feature off (no deploy label; the repo has no such gate).",
  "ui_review": true,
  "ui_review_paths": {
    "walt_ui": [
      "mobile/",
      "backend/apps/walt_ui/lib/walt_ui_web/live/"
    ]
  },
  "_ui_review": "true = an MR whose diff touches any path in ui_review_paths[<repo>] is NEVER auto-merged. When it goes green, tick verifies it VISUALLY and waits for the owner's explicit 'merge'. Mobile paths (mobile/) verify in the iOS simulator (sim.sh); web paths (see web_review_paths) verify in a browser. The session makes the change visible, screenshots it, opens the shot on the owner's Mac + Slack-pings him. Backend-only MRs (no ui_review_paths match) auto-merge normally. Paths are prefix-matched against `git diff --name-only origin/main...HEAD`. sim_device (optional) pins a simulator udid.",
  "domain_owners": {
    "walt_ui": [
      {
        "domain": "enrichment",
        "name": "Erich",
        "slack": "@U0BE9N6K50R",
        "paths": [
          "backend/apps/walt_ui/lib/walt_ui/enrichment/"
        ]
      }
    ]
  },
  "_domain_owners": "The human who owns a domain hears about changes in it: when an MR opens whose diff touches any of a domain's paths (prefix-matched against `git diff --name-only origin/main...HEAD`), tick Slack-DMs that owner once per dispatch (marker owner-notified-<domain>). An FYI, never a gate \u2014 the MR still reviews and merges on its own (process/cross-domain-proceed.md). `slack` takes a @handle or @Uxxxx id. Adding an owner is a config edit; no person is ever named in code.",
  "web_review_paths": {
    "walt_ui": [
      "backend/apps/walt_ui/lib/walt_ui_web/live/"
    ]
  },
  "_web_review_paths": "Subset of ui_review_paths that is WEB UI (Phoenix LiveView), verified in a browser instead of the iOS sim. A match routes the visual review to a headless-browser screenshot of the rendered page.",
  "web_app_url": "http://localhost:4000",
  "web_review_routes": {
    "walt_ui": [
      {
        "match": "backend/apps/walt_ui/lib/walt_ui_web/live/app/integrations",
        "route": "/settings/integrations"
      }
    ]
  },
  "_web_review": "web_app_url is the local dev base URL the browser review opens (so Margie has eyes on the same rendered page the owner does). web_review_routes[<repo>] maps a diff path prefix to the exact route to verify \u2014 e.g. an Integrations LiveView change is verified at http://localhost:4000/settings/integrations. First matching route wins; no match falls back to web_app_url. Nothing hardcoded in the scripts \u2014 set the base/route per project here.",
  "local_dev_repo": "walt_ui",
  "local_dev_service": "app",
  "local_dev_assets_cmd": "mix esbuild app && mix tailwind app",
  "local_dev_migrate_cmd": "mix ecto.migrate",
  "_local_dev": "localdev.sh runs the local dev web app via DOCKER (never native mix/asdf/direnv). local_dev_repo (default regression_repo/default_repo) + repo_subdirs[<repo>] locate the backend; local_dev_service is the compose service (app); web_app_url is the URL it polls; local_dev_assets_cmd is run in the container if the JS bundle is missing; local_dev_migrate_cmd applies pending migrations (the usual 503 cause after a pull). localdev.sh up|status|logs|restart.",
  "session_autoanswer": true,
  "_session_autoanswer": "the sessions poller answers Claude Code permission prompts in Margie's coding sessions with yes on the owner's behalf; prompts that look dangerous (force push, reset --hard, secrets, deploys, sudo, curl|sh) are escalated instead.",
  "review_max_rounds": 4,
  "_review_max_rounds": "After this many review rounds that still request changes on one MR, Margie pings the owner (the fix loop keeps going \u2014 this is a heads-up, not a stop). A local review re-runs on EVERY new commit; only an actual approve verdict merges (round count never does), so there is no round cap that could freeze a fixed MR at request_changes.",
  "review_effort": "high",
  "_review_effort": "Reasoning effort for the local charter review (the merge safety gate). Higher than qa_effort on purpose \u2014 a review that hallucinates a finding (e.g. 'no test exists' on a diff that contains the test) either blocks a good MR or lets a bad one through. The review prompt also forces it to list+read the test files before any coverage finding.",
  "regression_scan": "fix",
  "_regression_scan": "regressions.sh proactively hunts regressions in Margie's OWNED code (everything her commits have touched) using the SAME reviewer charters as MR review, and routes fixes through dispatch.sh (QA + charter review + UI/backend gate). off = disabled; scan = find + report to the owner; fix = also dispatch a fix per confirmed/likely regression (one at a time; backend auto-merges, UI holds for the owner). regression_scan_time (daily, weekdays), regression_since_days (ownership + change window, default 14), regression_repo (default = default_repo).",
  "regression_scan_time": "09:30",
  "regression_since_days": 14,
  "regression_repo": "walt_ui",
  "dispatch_budget_usd": 4,
  "task_budget_usd": 5,
  "daily_budget_usd": 25,
  "_dispatch": "dispatch.sh: the ticket/test-case/use-case/requirements data sources and defaults for filing; repo_subdirs maps a repo to the sub-project sessions run in; planner_model/qa_model override the claude CLI default (empty = default). Connect every DB above to the Notion integration or the pipeline can't see them.",
  "agent_identity": "Margie",
  "agent_roster": "~/path/to/repo/.claude/agent-messages/roster.json",
  "agent_messages_ds": "\u2026data source id (defaults to the roster's db entry)\u2026",
  "_agent_messages": "agent-messages.sh: this harness's name in the shared Agent Messages Notion DB, the committed roster (owners, Slack/Notion ids), and the DB. Identity file ~/.claude/agent-messages-identity wins over agent_identity.",
  "contacts": {
    "wife": "+15551234567",
    "mom": "jane@icloud.com"
  },
  "_contacts": "Spoken alias \u2192 iMessage handle (phone/email is most reliable) for messages.sh.",
  "notion_owner_person_id": "<owner Notion person id \u2014 see notion.sh whoami / the users API>",
  "review_agents": [
    "code-reviewer",
    "adr-reviewer"
  ],
  "hologram": "on",
  "hologram_monitor": "",
  "hologram_avatar": "",
  "hologram_quilt_size": 0,
  "hologram_target_diam": 0.55,
  "hologram_fovy": 14,
  "hologram_depthiness": 1.0,
  "hologram_frame_y": 0,
  "hologram_focus_z": 0.09,
  "hologram_hair_color": "",
  "hologram_env_intensity": 0.35,
  "hologram_light": 1.0,
  "hologram_lipsync_offset": 0,
  "hologram_caption": "on",
  "_hologram": "Margie on a Looking Glass display (tested on the Go, 1440x2560). Margie.app opens a borderless window on the display when one is connected (hologram: off disables it; hologram_monitor pins it as WxH or a 0-based index). Calibration is read from Looking Glass Bridge (install it from look.glass/bridge and open it once with the display attached) and cached in ~/.margie/lkg/. The character is a VRM avatar at hologram_avatar (default ~/.margie/avatar/margie.vrm; scripts/avatar.sh fetches a placeholder, VRoid Studio makes a custom one). Framing: hologram_target_diam is the size of the in-focus volume in metres (bigger = smaller Margie), hologram_fovy the vertical FOV in degrees, hologram_depthiness scales the view cone (more depth, more ghosting), hologram_frame_y shifts the focus up/down from the head bone and hologram_focus_z moves the focal plane forward onto the face (the head bone is inside the skull; anything off the focal plane blurs). hologram_hair_color (CSS colour) recolours the model's hair materials, e.g. '#c8c8d0' to grey a placeholder. hologram_env_intensity scales the studio environment light and hologram_light the key/fill/rim lights (raise if she looks dark, lower if washed out). hologram_quilt_size 0 = the display's native quilt (4092 on the Go); set 2046 if the window can't hold ~30 fps. hologram_caption: off hides the spoken-text captions. Lip-sync follows ElevenLabs' per-character timestamps (OpenAI/system voices fall back to loudness); hologram_lipsync_offset (seconds) shifts the mouth earlier (negative) or later if the display lags the speakers.",
  "_secrets_note": "Any value may be a 1Password reference like op://Vault/Item/field \u2014 resolved at runtime via the op CLI (sign in with op signin, or set OP_SERVICE_ACCOUNT_TOKEN for headless). Plaintext values still work.",
  "slack_ping": "on",
  "agent_autoreply": "on",
  "agent_autoreply_per_hour": 6,
  "_slack_extension": "slack_ping on|off: Jev-chosen background notices DM'd to the owner (tom-ping.sh). agent_autoreply on|off + agent_autoreply_per_hour: Margie answers other agents herself (agent-messages.sh auto), read-only, NEEDS TOM flagged.",
  "epic_parallel": 2,
  "max_coding_sessions": 4,
  "evals": "on",
  "_parallel_and_evals": "epic_parallel: how many tickets of one epic may code at once (disjoint files, dependencies merged); max_coding_sessions: overall cap on live coding sessions. evals on|off: nightly answer evals (evals.sh auto)."
}
